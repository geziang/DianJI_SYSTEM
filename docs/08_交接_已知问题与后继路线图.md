# 08 · 交接：已知问题与后继路线图

> 交接日期：2026-09-14（2026-09-15 深夜大更新：编码器欠流说谎案终审入档）｜**系统定位**：实验室统一维护的分层电机底层系统——功能主体已验证（标定/固化/电流环 FOC-4），换芯片只动板级/驱动两层（适配指南=仓库 README §9），上位机与上层算法按接入契约叠加（=README §10）
> 定位：给实验室后人的"地面实况"——正式规格见 SPEC-*，本文件只记**实况、坑、和下一步**。

## 1. 已知问题登记表（按优先级）

| # | 问题 | 状态 | 影响与动作 |
| --- | --- | --- | --- |
| 1 | **编码器欠流说谎（头号开放问题，2026-09-15 终审）**：差分电流一流动（0.06A 即触发），MT6701 输出"方向与转矩一致、幅度虚高 3~5 倍"的假角度流；电流归零立刻恢复诚实。**换芯片/换供电/传输校验/标度检验/静态误差五层排除全灭**（排除链见经验13 §2）——根因在传感器模拟前端或其物理环境（磁耦合/地弹/采样拍频三选一待仪器判决） | **开放，硬件级** | **速度环因此挂起**（代码完备+九层防御就绪）；判决实验清单见 §1.6（按性价比排序，首选试 20kHz PWM） |
| 1.5 | **接线规矩（实验室铁律）**：① ST-Link 永远只接 **SWDIO/SWCLK/GND 三根**（供电型棒的 3.3V 禁接自供电目标——本台 ST-Link 已实测供电型）；② 核心板 USB 与转接板 Type-C **不同插**；③ **H7 是裸电源排针**=并联入口；④ Type-C 无 CC 下拉用 A-to-C 线；⑤ 三线铁律已实测生效（掉线类故障清零） | 长期执行 | 原理图证据链见经验11 |
| 1.6 | **欠流说谎判决实验清单（交给硬件组/有仪器者）**：① **PWM 试 20kHz**（.ioc ARR 7199→3599，谎言消失即得可用配置，性价比最高）；② 示波器 AC 探编码器 VCC/SDA（使能瞬间）；③ 编码器外移 5~10cm（磁耦合判决）；④ VCC-GND 并 100nF+10µF；⑤ PCB 化（§3 清单） | 待执行 | 任一实验出结果即收窄 §1 三机制 |
| 2 | **诊断姿态现状**：编码器轮询**已回 1ms**（换向质量所需）；有效门 50ms、估计窗 100ms、九层防御栈（经验12 §2 全表）——这些不是负担是**资产**，欠流说谎修复后全部保留 | 长期 | 防御栈零成本常驻；`RUN_IQ_SPEED_SIGN=-1` 本机符号约定勿删 |
| 3 | 实验台连接脆弱（杜邦线/转接板插针）：EMI 与接触问题高发，本周全部硬件故障的总土壤 | 持续 | 见 §3 硬件组需求清单；换编码器时**接线按原理图复核**再上电 |
| 4 | I2C 400kHz 升频计划**已反转** | 决策记录 | 旧文档（2026-09-13 变更记录）提过 Phase B 升 400kHz——边际走线下低速更稳，勿执行 |
| 5 | 测试 target ROM 余量仅 1136B | 持续 | 加代码前 `_build_cli.cmd` 量尺寸；候选腾挪方案见 00_文档总览 2026-09-13 ROM 瘦身条目 |
| 6 | NFR-3 偏离：R4.1 速度估计器 PC 单测 owner 豁免暂缓 | 待补 | `speed_estimate.c` 已按零 HAL 可测形态写好，补 `tests/pc/test_speed_estimate.c` 即可销项 |
| 7 | CubeMX 可正常再生成（.ioc 已与现实同步，2026-09-14 闭环） | 已解决 | 纪律：关 Keil → 生成 → `git status` + 双构建 → 提交；工程文件手工编辑必须文本级+保 BOM |

## 2. 后继路线图（按建议优先级）

### 2.1 速度环收尾（被问题 #1 阻塞，硬件判决后立即解锁）

**状态**：代码完备（估计器+PI+斜坡+保护+九层防御，`git log 2026-09-15`），PI 保守值 kp=0.0015/ki=0.008 已就位，速度空间 450/580rpm 已放开。**唯一阻塞=编码器欠流说谎**——真值一恢复，流程为：

1. `p → s → +` 阶梯整定（振荡按 `2`、偏软按 `1`、稳差按 `3`）；
2. FR-4.5 四项指标回填 SPEC-RUN；
3. git 打 `v0.5-demo-handover` 终版标签。

（整定数据须取自无 `[FB SUSPECT]` 的可信窗口——防御栈会把可疑段标出来。）

### 2.2 R3 剩余（运行态完善，证据已立项）

| 项 | 内容 | 证据/动机 |
| --- | --- | --- |
| ① ISR 内连续出角 | 电流环 ISR 内直接喂编码器角，替代 1ms poll 喂 forced angle | FOC-4 验收实测 id 漂移 ≈ iq*·sin(14°)（293rad/s 时 0.1-0.2A），理论-实测已互证；术前基线数据在 FOC-4 验收报告 §5.1 |
| ② 遥测拍均值 | 遥测行用 `foc_runtime_consume_tick_mean`（现成 API）替代瞬时值 | 消 500ms 瞬时采样混叠毛刺（FOC-4 测试中肉眼可见） |
| ③ FR-3.4 完整故障链 | 补 ADC 饱和/控制周期超时 | 骨架仅覆盖编码器丢失 |
| ④ FR-3.5/3.6 | 遥测定频与带宽预算（115200 下的行数上限）；指令通道抽象 hook | SPEC-RUN §5 原文 |

### 2.3 R5 及以上（新里程碑）

- **位置环（FOC-6）**：SPEC-RUN §7 已立边界（位置环输出速度目标、不得直接给 iq/操作 PWM）——**上层算法项目从这里接入**（读 README §10 契约）；
- **CAN 总线/多关节**：当前零代码，属机器人整机层需求；FR-3.6 注册式指令 hook 是它的固件侧预留；
- **上位机**：速度环遥测行格式（`[RUN] id/iq/n/n*`）已可被脚本解析，上位机从此处接入；
- **多芯片适配**：按 README §9 五步指南（板级+驱动两层），首个新芯片适配建议从同家族（F103→F103 高型号或 F4）起步，跑测试固件全序列做回归。

### 2.4 建议不做 / 慎做

- **不再升 I2C 频率**（问题 #4）；
- 测试固件不再加大功能（ROM 余量问题 #5），新功能进 F103-run（28KB 余量）；
- 冻结基线目录勿改（回退锚点，readme 有变更记录制度）。

## 3. 硬件组需求清单（本周硬件斗争的教训凝结）

| # | 需求 | 动机（本周实证） |
| --- | --- | --- |
| 1 | 编码器线改**带屏蔽双绞/排线+可靠连接器**（告别杜邦线） | I2C 间歇失败/深挂的土壤；摇线试验方法论见经验10 |
| 2 | 编码器供电**独立 LDO/强去耦** | 受损编码器拖垮 3.3V 轨连带 ADC 劣化——单点故障变全系统故障 |
| 3 | 板间连接（转接板↔驱动板）**固定连接器化** | 接触劣化同时打击 I2C 与模拟采样（共地路径） |
| 4 | PCB 化整机（或至少 MCN 板） | 三块板+飞线的台面是全部 EMI 问题的放大器 |
| 5 | 预留编码器**机械固定+磁环间隙规范** | 振动下磁环/接线松脱（455rpm 急停×2 后开始劣化） |

## 4. 交接清单（交接时的实物+数字资产）

- [x] git 仓库（本地；**建议后人第一件事：加远程备份**，Gitee/GitHub 私有）
- [x] 双 target 0E/0W 构建（_build_cli.cmd 可复验）
- [x] 参数 Flash：页 A rev=4（新编码器 offset=1.4255，learned_R 链延续）
- [ ] 硬件一袋：驱动板/转接板/ST-Link/线材/**编码器（已损坏，注明）**/电机
- [ ] 编码器换新 + 验收链通过 + FR-4.5 回填 + `v0.5-demo-handover` 标签（交接后首件事）
